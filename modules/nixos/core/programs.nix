{
  ...
}:

{
  # Firefox is kept as an always-available browser in addition to user packages.
  programs.firefox.enable = true;
  # Zsh is the default interactive shell for the main user.
  programs.zsh.enable = true;
}
